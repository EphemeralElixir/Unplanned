import React from 'react';

const Splash = function Splash() {
  return (<div>
    <h2> Let's build out the Splash Component </h2>
  </div>);

  // Build out some kind of a redirect so that it changes state to the main App???

  // return (<div>
  //   <button type="button" onClick={window.socket.api.login}>Login with facebook </button>
  // </div>);
};

export default Splash;
